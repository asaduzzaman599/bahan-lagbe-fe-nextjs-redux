import Loading from '@/components/ui/Loading'
import React from 'react';

const LoadingPage = () => {
    return (
        <div>
            <Loading />
        </div>
    );
};

export default LoadingPage;